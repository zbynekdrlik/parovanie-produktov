## 2026-07-22 — #49 partial-batch upload-pairings mark-as-uploaded fix (worker)
- Bug (adversariálny review PR #48): `_do_upload_pairings` po úspešnom importe označil za „uploaded" VŠETKY `new_keys`, aj tie čo v `link_rows` vygenerovali 0 riadkov (produkt bez variant kódov, alebo kód zdedupovaný ako „seen"-loser inej položky tej istej dávky) — bezkódová položka sa navždy stratila, žiaden budúci beh ju neskúsil znova.
- Validated (verify-issue-still-valid): PR #133 (#109) len extrahoval `_do_upload_pairings` z pôvodného n8n endpointu, logiku okolo `_save_uploaded` nezmenil — bug stále reprodukoval na aktuálnom kóde.
- RED `4a78718` (`test_pairings_partial_batch_only_marks_keys_with_rows_uploaded` — 2 kľúče, jeden s kódom jeden bez; potvrdené zlyhanie pred fixom `assert count==1` → `2` cez git-stash overenie) → GREEN `7555f39`: `uploaded_keys` odvodené z `written_codes = {r[0] for r in rows}` ∩ `variant_codes`, len tie sa zapíšu do `uploaded_pairings.json`; zvyšok ostane „nový" (retry) a ráta sa do nového `blocked` počtu (mirror existujúceho celo-dávkového blocked prípadu). `_do_upload_suppliers` bug nemá (1:1 code→row, žiadna indirekcia) — overené čítaním `supplier_rows`.
- Verzia 0.53.0→0.54.0. 71/71 relevantných testov (`test_webreview.py`+`test_webreview_parovania_eshop.py`), 447/447 celkovo, ruff clean. PR #137 (Closes #49), merge `6555386`, main CI zelené, nasadené v0.54.0 (systemctl restart). Data-safety PRED==PO (decisions 2831, ordered 146, order_pairings 57, waiting 11, supplier_assignments 1, uploaded_pairings **1782**, users 3). Live dry-run overenie na skutočných dátach: 415 nových párovaní, blocked=0, dry_run nezmenil `uploaded_pairings.json` (md5 zhoda). DOM `[data-testid=version]`=v0.54.0.
- Follow-up docs-only PR #138 (playbook gotcha do `.claude/skills/webreview/SKILL.md`, verzia 0.54.1) — merge `51e1d8f`, nasadené a overené (DOM v0.54.1, store counts nezmenené).

## 2026-07-22 — #109 in-app „Párovania → eshop" automation (migrácia n8n YuDugCCOnwejRfva) (worker)
- Migrácia denného n8n workflowu (21:00 → upload-pairings → upload-suppliers → Discord súhrn) do appky ako automatizácia na generickom runneri (#93). Verzia 0.50.0→0.51.0. RED tests `20e0ef4` → GREEN implementácia (tento commit).
- **Architektúra (NEkopíruj logiku):** jadro oboch n8n endpointov vyextrahované do zdieľaných funkcií `_do_upload_pairings(dry)` / `_do_upload_suppliers(dry)` → `(result, status)`; HTTP endpointy robia len auth + delegujú; nová `run_parovania_eshop()` volá OBE jadrá priamo (žiadny self-HTTP, žiadny bearer — in-app migrácia zároveň eliminuje exposed-bearer dlh nočného pushu). Existujúcich 40+ upload/endpoint testov ostalo zelených (identický výstup).
- **Bezpečnosť:** automatizácia PÍŠE do živého eshopu → registrovaná `enabled=false` (štartuje Zastavené, beží len po ▶ Štart; deploy nikdy auto-nepushne). Idempotentná cez `uploaded_pairings.json`/`uploaded_suppliers.json` (re-run nič dvakrát nenahrá). Číta LEN manažérove decision/assign stores (čo pushnúť), nikdy ich nemení.
- **Stavy pre tab:** `last_result.status` = ok/blocked/failed (mirror n8n Sprava node) — blocked=napárované bez kódov, failed=import rc!=0/lock/timeout; genuine exception → runner `last_status='error'`, app žije. Tab `parovania_eshop` (status-only, vzor renderShoptetSync): Beží/Zastavené pill, ▶ Štart/⏹ Stop (persist), ⚡ Spustiť teraz, per-beh farebný `.autoresult` box (párovania+dodávatelia počty). Cache-bust v38→v39.
- Testy: 11 backend (`test_webreview_parovania_eshop.py`) + 3 e2e (`tests/e2e/test_parovania_eshop.py`) — registrácia/disabled, push oboch + počty, idempotencia, blocked/failed/error degrade, disabled netiká, reads-not-writes manager stores. 446 backend + 38 e2e zelené, ruff clean.
- Po nasadení: n8n workflow `YuDugCCOnwejRfva` treba UNPUBLIShnúť (konvencia #103) — spraví supervisor.

## 2026-07-22 — #118 sidebar „Eshop" folder + refinements (worker)
- Rozbaľovací priečinok „Eshop" (predošlý worker, PR #131) + Marekove upresnenia (comment 2026-07-22): (1) admin „Užívatelia" presunutý MIMO priečinka na samostatný `#usersNav` úplne dole (nad tmavým módom/verziou), (2) celá sekcia „Čoskoro — rozšírime" + 4 „soon" placeholdery + `.soon`/`.soonpill`/`.soon-nav` CSS ZMAZANÉ, (3) priečinok „Eshop" ponechaný (collapsible, persist `folder:eshop`) s pracovnými tabmi + „Automatizácie".
- `visibleTabs()` → `isAdmin()`; `renderTabs()` teraz plní 3 kontajnery (`#tabs`/`#autoTabs`/`#usersNav`), `.users-nav:empty{display:none}` skryje prázdny (non-admin). Selektorový kontrakt (`#tabs` v `.sidebar`, accessible names, `.navcount`) zachovaný — auth testy bez zmeny. Cache-bust v36→v37.
- Testy: `test_nav_order_has_review_last` upravený (#tabs bez „Užívatelia"); nový `test_users_standalone_at_bottom_and_no_soon_section`. Refinements commit `4c1e364`.
- **De-flake** `test_sidebar_hosts_nav_and_pagetitle_updates` (`6b9810a`): `wait_for_selector('[data-testid=version]')` NEgatuje init (span je v DOM od štartu); `#pageTitle` má statický „Kontrola párovania" markup čo `init()` async prepíše → race (green push / red pull_request TOHO istého commitu). Fix: wait na `.sidebar #tabs button` + title. Stale-comment fix `888d9fd`. Playbook (webreview SKILL.md) doplnený o oba gotchy.
- 32 e2e + 418 backend zelené; ruff clean. PR #131 (Closes #118), merge `9ccfaa5`, nasadené v0.48.0. Post-deploy živý forestshop.newlevel.media (admin): priečinok „Eshop" zoskupuje taby + collapse/expand + persist, „Užívatelia" samostatne dole (klik → tab funguje, 19 user-rows), žiadna „Čoskoro", 0 console errorov, DOM `[data-testid=version]`=v0.48.0. Data-safety PRED==PO (decisions 2831, ordered 146, order_pairings 57, waiting 11, supplier_assignments 1, users 3).

## 2026-07-22 — #91 auth (worker)
- #91 prihlasovací systém: RED 52ed16a → GREEN 62677d2 (22-test auth suita: gate/session/CSRF/rate-limit/reset-tokeny/admin CRUD/bootstrap); review fixes 79c3592 (dummy-hash timing) + e05c478 (CSRF bytes, SMTP degrade, rate-limit prune, +2 testy). PR #104, merge 9db7adb, nasadené v0.44.0 (DOM overené na živom webe, PRED==PO stores, n8n bearer intact).
- Bonus bug (pre-existing v0.42): [hidden] vs display:flex — search box presakoval do všetkých tabov; RED 76271be → GREEN bb3354f.
- Rozhodnutia: /api/n8n/* mimo session gate (vlastný bearer); reset-token store hashovaný sha256; bootstrap create-if-missing; legacy testy = reálna session (authed_client + store seed), e2e autouse cookie + @pytest.mark.anonymous.
- Otvorené: #113 SMTP údaje pre reset-mail (needs-answer — čaká na Mareka).

## 2026-07-22 — #94 rebrand „Forestaci" + nová URL (worker)
- Prebrandované z „Forestshop" na „Forestaci": sidebar `.brandtxt` + `<title>` v index.html AJ auth shell (auth.html; login/forgot/reset ho dedia). Eshop odkazy forestshop.sk NEDOTKNUTÉ; email subject „Párovanie Forestshop" ponechaný (ambiguous eshop-ref → default keep). Verzia 0.44.1→0.45.0. Commit 621e8fa, PR #116, merge e896040, nasadené v0.45.0.
- Brand-lock test tests/test_branding.py (index authed + login shell anonymous; pozit. + negat. assert). E2E fixture: pinnutý AUTH_COOKIE_SECURE=0 do _AUTH_ENV — lokálne e2e padali CSRF/400 na dev boxe s reálnym data/.auth_env (AUTH_COOKIE_SECURE=1 → Secure cookie sa cez http://127.0.0.1 nevráti); CI to nemal (žiadne data/). Deterministické lokálne e2e.
- Cloudflare (aditívne, API token data/.cf_token): ingress tunela a3df5493… + DNS CNAME forestaci.newlevel.media → tunel (proxied). parovanie-forestshop.newlevel.media OSTÁVA (obe → localhost:8801). Post-deploy: obe URL anon `/`→302 /login, /login 200, /api/version v0.45.0; DOM brand „Forestaci", [data-testid=version]=v0.45.0; post-login index sidebar brandtxt=Forestaci. Data-safety PRED==PO (decisions 2831, ordered 146, order_pairings 51, waiting 11, suppliers 1, instock 10, users 2).

## 2026-07-22 — #120 rebrand SPÄŤ na „Forestshop" + URL forestshop.newlevel.media (worker)
- Marek si to rozmyslel: brand názov appky späť z „Forestaci" na „Forestshop" (parciálny reverz #94). Tie isté 4 reťazce: index.html `.brandtxt` + `<title>`, auth.html brand `<b>` + `<title>`. Eshop odkazy forestshop.sk / Shoptet NEDOTKNUTÉ. Brand-lock test tests/test_branding.py otočený na „Forestshop" (pozit.+negat.): RED 3c47803 → GREEN f270935. Verzia 0.45.1→0.45.2. PR #122, merge 3b62047, nasadené v0.45.2.
- Cloudflare (aditívne): na ten istý tunel a3df5493 pridaný TRETÍ hostname forestshop.newlevel.media (ingress vložený PRED catch-all) + DNS CNAME proxied → localhost:8801. Reštart tunela NETREBA. Všetky tri URL žijú: forestshop (nová primárna), parovanie-forestshop (denná manažérova — MUSÍ ostať), forestaci (ponechaná, neškodí).
- Post-deploy (živý web, cez tunel): všetky 3 hosty /login 200, `/`→302 /login?next=/, /api/version v0.45.2. Real-browser DOM na forestshop.newlevel.media: login shell brand „Forestshop", title „Prihlásenie — Forestshop", [data-testid=version]=v0.45.2, žiadne „Forestaci". Post-login index (cez tunel, session): sidebar brandtxt=Forestshop, title „Kontrola párovania — Forestshop". Data-safety PRED==PO (decisions 2831, ordered 146, order_pairings 51, waiting 11, supplier_assignments 1, instock 10, users 3, unavailable 13).

## 2026-07-22 — #93 in-app automation runner + „Nevyzdvihnuté zásielky — Pošta SK" (worker)
- Generický runner (`automation_runner.py`: registry + thread-scheduler, stav `data/out/automations.json` 0600, default DISABLED, štart len v `__main__`) + prvá automatizácia = verný port n8n `2mhrdy0ouHe4VPeH` (`posta_uncollected.py` — čítané node-by-node cez MCP: notified+ZNP*, kadencia 0/+3/+3/+7 max 4, VŠETKY 4 maily ZÁKAZNÍKOVI, stav `count|YYYY-MM-DD`). Zdroj = vlastný orders export (packageNumber/email/phone/billFullName UŽ v ňom sú — Sheet netreba). invalid_format (13-14-miestne numerické štítky, ~57/281 reálnych zásielok — TO mesiac potichu rozbíjalo n8n; live-reprodukované na API) sa loguje + zobrazuje v tabe. Feature commit f487a4d + hardening 2752008 (eskalácia sa persistuje HNEĎ po sende — crash nesmie zajtra duplikovať mail; prázdny e-mail neskúša SMTP), PR #123 merge b303a8f, v0.46.0.
- Follow-up PR #124 merge 7ef02f3 (v0.46.1): admin link — live probe ukázal, že prehľad objednávok GET filter NEMÁ (`?query=`/`?code=` ignorované, POST-only); jediný deep-link = `/admin/vyhladavanie/?string=<kód>&src=orders` (vracia presne tú objednávku).
- Testy: 50 nových unit/integration (runner safety/persistencia/plánovanie; posta detekcia/kadencia/šablóny+escape; Flask auth-gate/toggle/full-run/SMTP-fail-retry/crash-mid-run/prune) + 3 e2e (`automations_server` fixture — hermetický „Spustiť teraz" s 0 zásielkami). Fixtures overené proti ŽIVÉMU api.posta.sk.
- Deploy: 2× reštart parovanie-web, DOM v0.46.0→v0.46.1, PRED==PO všetkých 9 stores (decisions 2831, ordered 146, order_pairings 51, waiting 11, supplier_assignments 1, instock 10, unavailable 13, users 3, grube 15). Živý toggle Štart→Beží (Ďalší beh 23.7. 09:00)→reload→Stop; finálny stav VYPNUTÉ (zapne Marek). n8n workflow ostáva aktívny — vypnúť po prevzatí (poznámka na #93).

## 2026-07-22 — #126 Nevyzdvihnuté zásielky filtruje LEN Pošta SK (worker)
- Rozhodnuté na #126 (Marek): DPD NEpridávať, automatizácia zostáva len Pošta SK — filtrovať zdroj zásielok podľa dopravcu z SHIPPING pseudo-položky exportu.
- Nález: reálny export NIKDY nepíše „Slovenská pošta" — Pošta SK domové doručenie sa volá „Kuriér" (223/228 objednávok, `EF…SK` formát čísla), DPD = „DPD doručenie na adresu"/„DPD kuriér" (14-miestne čísla). Allowlist „pošt"/„Balík" by preto vyradil 223/228 reálnych Pošta zásielok → implementácia je BLOCKLIST (dpd/gls/packeta/zásielkovňa/in time/wedo/spservis), objednávka bez SHIPPING riadku fail-open (zahrnutá). Zdokumentované v komentári na #126 + docstring `posta_uncollected.py` + `.claude/skills/webreview`.
- RED `0f15865`→GREEN (súčasť `56cb7b9`): `tests/test_posta_uncollected.py::test_shipments_dpd_carrier_excluded_posta_carrier_included`.
- Bonus (v scope, malý): `_send_mail_html` teraz defaultne BCC-uje `MAIL_BCC` (konvencia „BCC vždy" #105) keď volajúci bcc neuvedie; odstránený natvrdo zapísaný `POSTA_BCC` literál. RED `56cb7b9`→GREEN `8358fc0`: `test_send_mail_html_defaults_bcc_to_mail_bcc_env` (+2 ďalšie). Mimo scope: `_send_mail` (reset hesla) BCC nemá — filed #127.
- Verzia 0.46.2→0.46.3, PR #128 merge `edee661`, nasadené v0.46.3.
- Post-deploy: PRED==PO všetkých 11 stores. Živé overenie „Spustiť teraz" (bezpečné — `escalation=={}`+`uncollected==0` pred behom, `emails_sent==0` po behu): `checked` 21→13 (8 DPD vylúčených), `invalid` (nesledovateľné) 8→0, box „⚠️ nesledovateľných" úplne zmizol z UI, žiadne console errory.

## 2026-07-22 — #127 `_send_mail` (reset hesla) doplnené o BCC (worker)
- Doplnok k #126: `_send_mail_html` (automatizácie) BCC-uje `MAIL_BCC` už z #126; `_send_mail`
  (reset hesla, `/forgot` flow) BCC nemal — malá medzera (~7 riadkov), rovnaký vzor skopírovaný.
  Signatúra ostala 3-argumentová (bez `bcc=` parametra — jediný volajúci ho ani nechce vypínať).
- RED `ff86134` → GREEN `0967e6d`: `tests/test_webreview_auth.py::test_send_mail_bcc_to_mail_bcc_env_when_set`
  + `test_send_mail_no_bcc_when_mail_bcc_env_unset`. Playbook (webreview SKILL.md „BCC vždy" bullet)
  aktualizovaný, že teraz platí pre OBE mail cesty — `e63ba0e`.
- Verzia 0.46.4→0.46.5, PR #129 merge `3c09468`, nasadené v0.46.5.
- Post-deploy: DOM aj `/api/version` = v0.46.5, PRED==PO všetkých 7 stores (decisions 2831,
  ordered 146, order_pairings 57, waiting 11, supplier_assignments 1, users 3, reset_tokens 1).
  Live-overenie zámerne BEZ reálneho odoslania reset-mailu (aby sa nepošlo majiteľovi zbytočné
  BCC) — funkčnosť pokrytá unit testom; `/forgot` stránka načítaná (200, 0 console errorov).

## 2026-07-22 — #106 „Dodávateľský sklad" — in-app supplier scraper (worker)
- Migrácia n8n „Forestshop — Dodávateľský scraper" (`6kn7jzBXTjbmbiVa`) na in-app runner (#93):
  nová automatizácia `dodavatelsky_sklad`, denne 05:00, **default Zastavené** (veľa externých HTTP
  volaní + platený OpenAI → beží až po Štart).
- Pure logika `src/parovanie/supplier_stock.py` (bez siete/OpenAI): `links_from_export` (internalNote
  http + visible, dedup), `extract_static` (JSON-LD → og/product meta → text-keyword LEN pre 4 overené
  domény), `need_llm` (available ALEBO price None), `build_llm_messages`/`parse_llm_json`,
  `is_recently_checked` (stale-skip 20h). 96 % pokrytie.
- Flask: `run_supplier_stock` + `_fetch_supplier_html` + `_llm_extract` (OpenAI cez `requests.post`,
  kľúč z `data/.ai_env` cez `_load_env_file`, `response_format=json_object`), `/api/supplier-stock`,
  store `data/out/supplier_stock.json` (vstup pre #107/#108). Frontend: per-item tab (vzor `renderPosta`)
  + filtre (všetky/chyby/AI/dodávateľ); poll zovšeobecnený (`_reloadAuto`).
- Testy hermetické (mock supplier HTTP AJ OpenAI): `test_supplier_stock.py` (53) + `test_webreview_
  supplier_stock.py` (10) + e2e `test_supplier_stock.py` (3). RED-before-GREEN sa netýka (feature).
- Verzia 0.57.0, PR #142 merge `014e915`, nasadené v0.57.0.
- Post-deploy: DOM = v0.57.0, PRED==PO manager stores (decisions 2831, ordered 146, order_pairings 57,
  waiting 11, supplier_assignments 1). Živé overenie: tab „Dodávateľský sklad" existuje, default
  Zastavené, Štart/Stop + Spustiť teraz prítomné, plán denne o 05:00, 0 console errorov. Reálny scrape
  sa ZÁMERNE nespúšťal (hit by mnoho dodávateľských webov + minul OpenAI). Odložené: odpublikovať n8n
  `6kn7jzBXTjbmbiVa` po zapnutí in-app (ako #109).

## 2026-07-22 — #143 „Jemnejšie/tlmenejšie farby" — softer palette (worker)
- Šéf: zelená v menu príliš krikľavá; presné hex farby dodal cez komentár na #143
  (green `#6CAB68` rgb 108,171,104, red `#D14D3B` rgb 209,77,59).
- `--accent`/`--accent-hov`/`--accent-soft` (svetlý aj tmavý mód) preladené na tlmenú
  zelenú v ROVNAKOM odtieni ako `#6CAB68` (~116° hue, len tmavšia kde treba biely text
  na plnej výplni — kontrast overený WCAG luminanciou skriptom, ≥4.3:1 všade, väčšina
  ≥4.8:1). Boss's presný hex `#6CAB68`/`#D14D3B` použitý DOSLOVA na miestach, ktoré
  explicitne pomenoval: order-supplier chip `.todo`/`.done`, „Skladom"/„Nedostupné"
  to-order tlačidlá + ich riadkové pásiky. `.active` chip (oranžová) jemne stlmená
  `#f59e0b`→`#DDA43C`, ostala odlíšiteľná; ten istý amber teraz aj na „Počkať"/curoff/
  warnbox. Review-karta „Dobré" border + primárne tlačidlá prepnuté na novú zelenú.
  Jemne stlmené aj fialové (`to-pairsave`+`to-order` link, zjednotené na `#713AC7`) a
  tyrkysové (`to-supsave`, `#1F7A72`) tlačidlá pre celkovú súdržnosť.
- `tests/e2e/test_order_chips.py` — 3 presné rgb asserty prepočítané a upravené
  ((108,171,104)/(209,77,59)/(221,164,60)); `.claude/skills/webreview/SKILL.md`
  selektorový kontrakt aktualizovaný na nové hex/rgb.
- Verzia 0.58.1, PR #145 merge `226ab46`, nasadené v0.58.1.
- Post-deploy: DOM = v0.58.1, PRED==PO manager stores (decisions 2831, ordered 146,
  order_pairings 57, waiting 11, supplier_assignments 1). Živé overenie na
  parovanie-forestshop.newlevel.media (Playwright, sidebar + Na objednanie + Kontrola
  párovania, svetlý aj tmavý mód): menu accent už nie je krikľavý, chip-y/tlačidlá
  jemnejšie a čitateľné v oboch módoch, 0 console errorov.

## #115 — „Vývoj" tab (GitHub issues) + idea lightbulb (2026-07-22, v0.59.0)
- Commits: 68dddb2 (bump 0.59.0), ad070fe (feat), cde7f74 (paginate). PR #148 → main af08674.
- New standalone „Vývoj" nav (bottom, all logged-in users) lists repo GitHub issues (open+closed, PRs filtered); fixed bottom-right lightbulb (#ideaBtn) creates an issue → appears in list.
- Backend proxies ALL GitHub calls: token from data/.gh_env (GITHUB_TOKEN+GITHUB_REPO via _load_env_file), Authorization header ONLY — never to browser/URL/log. Endpoints /api/dev/issues (bounded 5-page pagination) + /api/dev/idea (title required/capped, rate-limited). Missing token → graceful „GitHub nedostupný", never 500. GITHUB_API_BASE env override enables hermetic e2e stub.
- Tests: tests/test_webreview_dev.py (11 backend, GitHub mocked); tests/e2e/test_dev.py + dev_server GitHub stub in e2e conftest (3 e2e). All existing tests green (535 backend + e2e).
- Live verify: forestshop.newlevel.media v0.59.0 DOM, tab lists 26 open + 28 closed real issues, lightbulb created #149 (closed after), 0 console errors. Manager stores PRED==PO (decisions 2831 / ordered 146 / order_pairings 57 / waiting 11 / supplier_assignments 1).

## #107 — „Riziko výpadku" JOIN-automatizácia (migrácia n8n 7ujLZ4WDNphSgsuj) (2026-07-22, v0.60.0, worker)
- Migrácia denného n8n workflowu (06:15, porovnanie nášho katalógu s dodávateľským skladom → Discord) do appky ako čisto READ-ONLY/advisory automatizácia — nikdy nezapisuje do eshopu. Verzia 0.59.0→0.60.0. Commits: `028a8e1` (bump), `196f7f5` (backend), `8ebd1ee` (frontend).
- **REUSE, nie re-scrape**: žiadna vlastná sieť/LLM — `run_riziko_vypadku()` len JOINuje on-disk export s UŽ napísaným `data/out/supplier_stock.json` (#106 automatizácia), cez rovnaký `internalNote` link kľúč. Pure jadro `src/parovanie/riziko_vypadku.compute_risk(csv_text, supplier_rows)`: risk = náš `export_helpers.state_of==1` (Skladom+visible/detailOnly) AND supplier row `ok=True, available=False`. Absencia dát (nikdy nescrapované, chyba, `available=None`) sa NIKDY nevykazuje ako risk.
- **`has_supplier_data` kontrakt**: keď #106 ešte nikdy nebežal (`rows==[]`), store nesie `has_supplier_data=False` a tab zobrazí „najprv spusti Dodávateľský sklad" namiesto zavádzajúceho prázdneho „0 rizík". Nový sub-vzor pre budúce JOIN-automatizácie zdokumentovaný v `.claude/skills/webreview/SKILL.md`.
- Registrovaná `Automation(key="riziko_vypadku", daily_at="06:15")`, **default DISABLED** (konzistencia s ostatnými, hoci je read-only). `GET /api/riziko-vypadku` (display) + `GET /api/riziko-vypadku/csv` (voliteľný download, `_csv_response`+`_csv_safe`). Tab „Riziko výpadku" (vzor renderDodavatelskySklad/renderPosta) — žiadne nové CSS.
- Testy: 12 pure-logic (`tests/test_riziko_vypadku.py`) + 10 Flask wiring (`tests/test_webreview_riziko_vypadku.py`, incl. manager-store isolation) + 3 e2e (`tests/e2e/test_riziko_vypadku.py`, `automations_server` fixture rozšírená o pred-vypočítaný `riziko_vypadku.json`). 557 backend + 50 e2e zelené, ruff clean.
- PR #150 (Closes #107), CI (test/e2e/version-check) zelené na push AJ pull_request evente, mergeable=clean.

## #108 — „Vypredané → Skladom" reštok (migrácia LIVE n8n KN1BE18HLdM8mfTc) (2026-07-22, v0.61.0, worker)
- Migrácia LIVE denného n8n workflowu (06:00, vypredané→skladom reštok + Discord) do appky ako automatizáciu (#93) + záložku namiesto Discord feedu. Verzia 0.60.0→0.61.0. Commits: `c66ac7e` (bump), `91782ce` (pure logika), `4ad0f3c` (wiring). PR #151 (Closes #108), merge `6485b0e`.
- **WRITE-JOIN vzor** (nový sub-pattern, na rozdiel od read-only #107): `restock_skladom.compute_candidates(csv_text, supplier_rows, now)` — náš produkt `vis=='visible'` A stav 2 (Vypredané) cez zdieľaný `export_helpers.state_of` A dodávateľ `ok && available is True && checkedAt čerstvé ≤48 h` (`MAX_PAIR_AGE_H`, presne n8n per-row check). Zápis = REUSE `import_builder.restock_rows` (OBE polia dostupnosti→Skladom, visible, stock 5, dedup) → `run_import` (ten istý careful import + #23 read-back ako n8n endpoint), žiaden self-HTTP.
- **Bezpečnosť prod zápisu**: default DISABLED (píše do živého eshopu); `has_supplier_data=False` keď #106 nikdy nebežal → tab „najprv spusti Dodávateľský sklad"; iba ok+available+FRESH flipne; idempotencia = stav-2-only detekcia (už-Skladom sa neflipne); zlyhaný import → `status=error` (nie tiché success), `_import_lock` busy → skip (nie dvojitý import), degraduje ako `parovania_eshop` (nehádže). Nikdy sa nedotkne manažérových decision stores.
- Registrovaná `Automation(key="restock_skladom", daily_at="06:00")` (po 05:00 scrape, pred 06:15 riziko). `GET /api/restock-skladom`. Tab „Vypredané → Skladom" (vzor renderRizikoVypadku) — žiadne nové CSS. Nový write-JOIN sub-vzor v `.claude/skills/webreview/SKILL.md`.
- Testy: 19 pure JOIN (`tests/test_restock_skladom.py`) + 6 `restock_rows` + 12 Flask wiring (`tests/test_webreview_restock_skladom.py`, incl. zlyhaný-import/busy/no-data/both-fields/izolácia) + 3 e2e (`automations_server` rozšírený o `restock_skladom.json`). 592 backend + 53 e2e zelené, ruff clean, coverage 93.6 %.
- Post-deploy overené naživo (v0.61.0 DOM): tab existuje, default **Zastavené**, ▶ Štart/⚡ Spustiť teraz prítomné, „najprv spusti Dodávateľský sklad" (na prode #106 ešte nebežal); NEklikané Štart/Spustiť teraz (prod zápis). Data-safety: decisions 2831/ordered 146/order_pairings 57/waiting 11/supplier_assignments 1 PRED==PO.
- n8n `KN1BE18HLdM8mfTc` (LIVE, active) — odloženo na unpublish až keď manažér zapne in-app verziu.

## #105 — „Pripomienky objednávok" (migrácia LIVE n8n MnskuiOdu3i5GKlF) (2026-07-22, v0.62.0, worker)
- Migrácia LIVE denného n8n workflowu (08:00, „Vybavuje sa" objednávky → Discord alert + AI klasifikácia + pripomienkový email) do appky ako automatizáciu (#93) + záložku namiesto dvoch Discord kanálov. Verzia 0.61.1→0.62.0. Commits: `2da21d5` (bump), `12b1b64` (pure logika), `7cda1ae` (wiring), `c7b3323` (frontend+e2e). PR #… (Closes #105).
- **AI-EMAIL automatizácia** (nový sub-vzor: klasifikácia + zákaznícky email, na rozdiel od scraper #106 / write-JOIN #108): `orders_reminder.select_orders(csv, now)` — „Vybavuje sa" dedup per code (prvý riadok vyhráva) A >4d. Čistá stavová mašina (vylepšenie z issue optim.#3): prázdna `shopRemark` → LEN červený alert (žiaden email); s poznámkou → AI klasifikuje. n8n posielalo email AJ prázdno-poznámkovým cez „BEZ POZNAMKY"→nekontaktovany — zámerne vypustené.
- **AI = jedno OpenAI volanie** (`_classify_contacted`, requests.post, gpt-4o-mini per #106 vzor, kľúč v Authorization hlavičke): faithful port n8n Text Classifier system promptu (minulý čas „volané"=kontaktovaný vs budúci „volať/budeme volať"=nekontaktovaný, +SMS, +dohoda „počká"). JSON `{"kategoria":...}`. Bez kľúča → „AI nedostupné", NEposiela naslepo, NEzapíše (retry). Chyba klasifikácie/SMTP per-obj → log+skip, beh nespadne.
- **Zdroj = existujúci `_orders_csv_cached()` CSV** (SHOPTET_ORDERS_URL, 30-min cache) — NIE XLS patternId=20; žiadna nová závislosť, rieši caching-optim z issue. `shopRemark` (stĺpec 28) = interná poznámka.
- **Dedup store `data/out/orders_reminder.json`** (`orders:{code:{status:emailed|skipped_contacted,date,...}}`, atomicky 0600, immediate-persist po každom emaili — crash safety ako posta). Objednávka už v store → neklasifikuje/neposiela znova (max raz/obj, zrkadlí n8n Data Table `forestshop_upozorneny_mail`).
- **Email** = faithful n8n HTML šablóna (subject opravený preklep „Foresthop"→„Forestshop"), cez `_send_mail_html` (From „Forestshop.sk", BCC MAIL_BCC default — „BCC vždy"). Admin deep-link cez `posta_uncollected.ADMIN_ORDER_LINK` (globálne vyhľadávanie — orders export nemá admin id).
- Registrovaná `Automation(key="orders_reminder", daily_at="08:00")`, **default DISABLED** (posiela reálne zákaznícke emaily + stojí OpenAI). `GET /api/orders-reminder`. Tab „Pripomienky objednávok" (červená/oranžová sekcia + počty + posledný beh, vzor renderPosta) — žiadne nové CSS.
- Testy: 20 pure (`tests/test_orders_reminder.py`) + 11 Flask wiring (`tests/test_webreview_orders_reminder.py`: no-note→red, contacted→skip, not-contacted→email-once+dedup, no-key→no blind email, classify/SMTP error→retry, BCC na drôte, manager-store izolácia, default disabled) + 3 e2e (`automations_server` rozšírený o `orders_reminder.json`). 625 backend + 56 e2e zelené, ruff clean.
- **Odložené na follow-up** (mimo scope): ručný override stavu per-riadok, inkrementálne cachovanie podľa code, in-app error alert pri zlyhaní fetchu.
- n8n `MnskuiOdu3i5GKlF` (LIVE, active, POSIELA reálne emaily) — odloženo na unpublish až keď manažér zapne in-app verziu (extra opatrnosť).

## #38 — nočný push aj `order_pairings` (inline páry mimo review setu) (2026-07-22, v0.63.0, worker)
- Validated (verify-issue-still-valid): #109 v čase issue-e ešte len tlačilo `decisions.json`; overené priamym čítaním `_do_upload_pairings` na `dev` — `order_pairings.json` sa nikde nenačítaval → issue stále platný, implementované.
- `import_builder.new_order_pairing_keys(order_pairings, uploaded)` — dedup pod `order:<code>` namespace v TOM ISTOM `uploaded_pairings.json` (review kľúč je vždy `SUPPLIER|pairCode`, nikdy sa nekríži). `_do_upload_pairings` teraz stavia `order_rows = order_pairing_rows(order_pairings, CODE2PAIR, exclude_codes={r[0] for r in rows})` a zapisuje `rows + order_rows` do JEDNÉHO `import_links.csv` (jeden import beh, mirror `/api/import` zip vzoru). Uploaded stav zapisuje LEN po reálnom (nie dry-run) úspechu, presne ako decisions. `run_parovania_eshop` (#109) to dostáva zadarmo (zdieľané jadro) — `pairings.order_count`/`order_blocked` pridané do jeho sumáru. UI tab „Párovania → eshop" zobrazuje `📦 Inline páry: +N nových … M prekrytých recenziou`.
- RED `8b15d13` (pure fn + push/dedup/exclude/dry-run/failure/wiring testy, KeyError na `order_count` cez celý súbor) → GREEN `61798f1` (implementácia) → `c1c1296` (self-review polish — 2 vysvetľujúce komentáre, no behavior change).
- Verzia 0.62.0→0.63.0. 631 backend + 56 e2e zelené (coverage 93.92 %, limit 80 %), ruff clean. PR #155 (Closes #38), merge `8069fb7`, main CI zelené (test+e2e; version-check skip na main), nasadené v0.63.0 (`systemctl --user restart parovanie-web`). Data-safety PRED==PO (decisions 2831, ordered 146, order_pairings 57, waiting 11, supplier_assignments 1, uploaded_pairings 2196). DOM `[data-testid=version]`=v0.63.0, konzola čistá.
- **Živý nález mimo scope #38** (zisté pri post-deploy overení, PRED-deployom zlyhanie): automatizácia `parovania_eshop` je na prode UŽ `enabled=true` (nie default-disabled ako popisuje playbook) a jej beh o 21:00 dnes zlyhal — Shoptet import na 415 produktov/1195 riadkov prekročil 120s `page.wait_for_url` timeout v `scripts/shoptet_import.py:296`; súčasne starý n8n workflow (`YuDugCCOnwejRfva`) volal ten istý endpoint a dostal 409 (lock kolízia — obe bežia na TEN ISTÝ čas 21:00). Filed as **#156**.

## #156 — veľký import „Párovania → eshop" na časti (120s browser timeout) (2026-07-23, v0.63.2, worker)
- Validated (verify-issue-still-valid): prod `data/out/automations.json` — `parovania_eshop` je `enabled=true`, beh 2026-07-22 21:00 `last_result.status="failed"` (pairings 415/1195 riadkov `ok:false`, suppliers `ok:false` kaskáda, `remaining:1075`); `grep chunk` = 0 → bug reálny, dev==main. Kolízny starý n8n workflow už supervisor unpublishol — riešim LEN timeout.
- Root cause: `_do_upload_pairings`/`_do_upload_suppliers` robili JEDEN `run_import(...timeout=900)` so VŠETKÝMI riadkami; `scripts/shoptet_import.py:296` čaká `page.wait_for_url(...timeout=120000)` — Shoptet spracúva 1195-riadkové CSV na serveri > 120 s pred presmerovaním → Timeout 120000ms.
- Fix (no-timeout-band-aids → chunking): nový zdieľaný `_import_rows_chunked(all_rows, header, dry, prefix, csv_safe, timeout)` v `webreview/app.py` — rozdelí na časti ≤`IMPORT_CHUNK_ROWS`=300 riadkov, jeden `run_import` na časť, `_import_lock` držaný cez celú dávku (release v `finally` → žiaden zaseknutý lock). PRVÁ zlyhaná časť zastaví dávku; `success_codes` (kódy z úspešných častí) gate-uje uploaded stav: decision kľúč sa zapíše LEN keď VŠETKY jeho kódy ⊆ success (idempotent + resumable + #49 partial-key ochrana); order kódy a suppliers 1:1. Nikdy neraise-uje TimeoutExpired (chytené → failed chunk). Na zlyhaní result nesie jasnú hlášku „import zlyhal na časti K/N (naimportované R z T riadkov)" → tab (`app.js` render `p.error`/`s.error`, cache-bust v46→v47).
- RED `1972c56` (4 testy: large-pairings split ≤300, large-suppliers split, mid-batch fail → failed+partial recorded+lock released, small=single import — 3 fail pred fixom) → GREEN `1f4021f`. Verzia bump `1e23976` 0.63.1→0.63.2.
- 635 backend + 10 e2e (parovania+shell) zelené, ruff clean. Reštok cesty (`run_restock_skladom`, `/api/n8n/shoptet-import`) majú tú istú triedu rizika → filed **#158**. PR/merge/deploy v0.63.2 — viď completion report (data-safety PRED==PO, DOM verzia).

## #158 — reštok + n8n reštok-import na časti (rovnaké 120s timeout riziko) (2026-07-23, v0.64.0, worker)
- Validated (verify-issue-still-valid): `grep -n "chunk" webreview/app.py` pred fixom ukázal len #156-ové call-sites (`_do_upload_pairings`/`_do_upload_suppliers`); `run_restock_skladom` a `n8n_shoptet_import` stále volali jediný `run_import(...)` na celú dávku → issue stále platný.
- Fix: obe cesty presmerované na ten istý zdieľaný `_import_rows_chunked` (#156) — `run_restock_skladom` zahodilo vlastné `_write_restock_csv`/`tempfile.mkstemp` a volá `_import_rows_chunked(rows, RESTOCK_COLS, False, prefix="restock_", timeout=900)` pod `_import_lock` (release vo `finally`); chybová hláška teraz nesie „časti K/N (naimportované R z T riadkov): <detail>" — rovnaký tvar ako n8n endpointy. `n8n_shoptet_import` necháva `sanitize_csv(raw_path, out_path)` zapísať CELÝ audit súbor (prežíva pre debugging), potom `out_path` prečíta späť (`csv.reader`) na riadky pre `_import_rows_chunked` — nečíta raw upload druhýkrát. Per-request `TimeoutExpired`/504 vetva zmizla (timeout sa teraz chytá VNÚTRI chunkera → 502 „import zlyhal" ako u pairings/suppliers — žiadny existujúci volajúci nezávisel na 504).
- RED (`b6fb970`): 4 testy — `test_large_restock_batch_split_into_chunks`, `test_restock_mid_batch_chunk_failure_records_error_and_releases_lock` (`tests/test_webreview_restock_skladom.py`), `test_n8n_import_large_batch_split_into_chunks`, `test_n8n_import_mid_batch_chunk_failure_returns_502_with_progress` (`tests/test_webreview.py`) — 650-riadkové dávky, mock `run_import`, 4 fail pred fixom (2 kvôli chýbajúcemu splitu, 2 preto že OLD kód nikdy nedosiahol 2. volanie). Tiež opravené 2 existujúce testy, čo predpokladali starý `run_import(csv_path, dry_run=False)` signature bez `timeout` kwarg (`test_import_sanitizes_then_runs`, `test_import_dry_run_passthrough`) — po chunkingu `_import_rows_chunked` volá `run_import(..., timeout=...)` vždy. → GREEN (`e7c57d7`). Verzia bump `b834bc7` 0.63.2→0.64.0.
- Playbook: `.claude/skills/shoptet` — „VEĽKÝ import" gotcha aktualizovaná (všetky 4 zápisové cesty teraz chunkujú; `n8n_shoptet_import` audit-file-then-readback odlišnosť zdokumentovaná).

## #43 — candidates.json ↔ ai_verdicts.json join was positional (idx), not keyed (2026-07-23, v0.65.0, worker)
- Validated (verify-issue-still-valid): read the current join in `add_supplier_review_data.py`/`build_review_data.py`/`finalize.py` — all 3 still did `{v["idx"]: v for v in verdicts}` + `verds.get(i)` over `enumerate(recs)`; no stable key protected it → issue still real, whole pipeline (not just LUKO).
- Fix: new `candidates_io.join_verdicts(recs, verdicts)` — the single tested join point, keys strictly on `pair_key`. A verdict without `pair_key` = hard `SystemExit` (no silent positional guess); a verdict whose `pair_key` isn't in `recs` = skipped + logged (never applied to a different product); `idx` kept only as a display/order aid. Wired into all 3 consumers. `gather_luko.py` (the only script writing `ai_verdicts.json` directly) now writes `pair_key` per verdict. `build_verify_input.py` carries `pair_key` into `verify_input.json` so a future AI verify Workflow can echo it back. `.claude/skills/suppliers/SKILL.md` documents the new `{pair_key,idx,chosen_i,reason}` shape (pair_key mandatory).
- RED `bb4fc83` (4 tests in `tests/test_candidates_io.py`: reordered candidates.json → positional join proven wrong, keyed join proven right; a filtered list still lands each verdict correctly; unknown pair_key skipped; missing pair_key raises — `join_verdicts` didn't exist) → GREEN `fbf28d5` (implementation) + lint-fix `ac90864` (ruff F841 on the filtered-list test). Version bump `5d1b8a7` 0.64.0→0.65.0.
- 645 backend tests green, ruff clean. PR #161 (Closes #43), merge `7a5ad0e`, main CI green (test+e2e; version-check skipped on main). Offline-pipeline code only (scripts for adding suppliers) — live web app behavior unchanged by design; deploy (`systemctl --user restart parovanie-web`) is version-refresh only, data-safety PRED==PO (review_data.json 2857==2857). DOM `verzia v0.65.0` confirmed on the live login page via Playwright.

## #153 — Pripomienky objednávok follow-up: ručný override + inkrementálny beh + viditeľný alert (2026-07-23, v0.67.0, worker)
- Validated (verify-issue-still-valid): read the live `orders_reminder.py`/`app.py`/`app.js` — all 3 wishlist items from #105 were still genuinely missing (no override endpoint, `select_orders` full-scans every run with no code-level skip, no nav-level failure indicator) → issue still real.
- **1) Manual per-row override**: new `POST /api/orders-reminder/override` reuses the EXISTING per-code `orders_reminder.json` dedup store (no new store file) — `action=contact` marks a red (no-note) row as manually-contacted (terminal, no e-mail); `action=send` sends the reminder now, allowed from red OR skipped (overrides a wrong AI "already contacted" verdict), never from an already-emailed row. `renderOrdersReminder()` gained a SKIPPED section (previously computed but never shown) so the "wrong AI verdict" case is actually visible/actionable.
- **2) Incremental processing**: `orders_reminder.order_fingerprint`/`partition_incremental` — an already-terminal order (emailed/skipped_contacted) whose date+note fingerprint is unchanged since the last run skips re-classification/re-mail and reuses its last display row; a newly-eligible order (no prior fingerprint) or a not-yet-terminal retry is always fully (re)processed. Persisted as `orders_reminder.json["fingerprints"]`.
- **3) Visible failure indicator**: `navError(key)`/`.navwarn` badge in `_navButton()` — ANY automation (all 7, not just orders_reminder) whose `last_status==='error'` now shows a small ⚠ on its OWN sidebar nav item, visible from any page (`loadAutomations()` moved into the unconditional init() prefetch, was previously only fetched when that tab happened to be open). Per-tab `.autoerr` detail banner already existed pre-#153.
- RED `3dd1025`/`74e1d0d` → GREEN `b56c9ca`/`927f142`/`0d8abd2`. Self-review (own senior-reviewer pass — no subagent-dispatch tool available in this environment) caught a real concurrency bug: the override endpoint held the app's single GLOBAL `_lock` for the whole request INCLUDING the outbound SMTP call (~20s timeout), which would have stalled every other admin action on the site during a manual send — fixed in `4642094` (network call moved outside the lock, matching `run_orders_reminder`'s own pattern; a post-send re-check under the lock keeps a genuine concurrent double-click from double-recording). Also caught during CI: the e2e "send" test unintentionally reached the REAL `_send_mail_html` path via a dev box's real `data/.mail_env` (the file is read by absolute repo path, independent of the fixture's isolated `WEBREVIEW_OUT`) — fixed in `b2a16fe` by pinning `MAIL_HOST=""` in the `automations_server` e2e fixture (deterministic, hermetic on every machine).
- 673 backend + 59 e2e green, ruff clean. PR #164 (Closes #153), merge `086b6b2`, main CI green. Deploy = `systemctl --user restart parovanie-web` on `main`; data-safety counts PRED==PO (`ordered_items` 146, `order_pairings` 57, `waiting_items` 11, `supplier_assignments` 1, `automations` 4 — all unchanged; `orders_reminder.json` still absent, automation never run in prod). DOM `verzia v0.67.0` confirmed live on `parovanie-forestshop.newlevel.media`; nav badge mechanism confirmed correctly wired (absence-when-healthy verified against the real prod `/api/automations` — no automation currently in error state, so the positive ⚠ case is proven by the e2e suite, not live prod); override endpoint confirmed live via a safe 404 probe (unknown code, no real order touched).

## #135 — our_images: periodicky validovať/čistiť mŕtve forestshop-CDN URL (2026-07-23, v0.68.0, worker)
- Validated (verify-issue-still-valid): the #50/#74 `onerror` placeholder was already live (checked `app.js`), and the ticket's own live finding (2026-07-22) already proved Chrome still logs "Failed to load resource" regardless of `onerror` — issue still real, no existing validation of `our_images` anywhere in the codebase (`grep image_health` = 0 hits before this PR).
- **New pure module `src/parovanie/image_health.py`**: `needs_check`/`record_result`/`is_dead`/`filter_dead`/`clean_products` — a URL is dead only after 2 CONSECUTIVE HEAD failures (a success resets the streak to 0 immediately), so a transient CDN blip never wipes a genuinely-good image; a confirmed-healthy URL is skipped for 24h (freshness window), but a URL that failed last time is ALWAYS re-checked next run regardless of freshness.
- **New in-app automation `image_health`** ("Kontrola obrázkov" tab, status-only like `shoptet_sync`) HEAD-checks (GET-Range fallback for hosts without HEAD support) every `our_images` URL currently in `review_data.json` and maintains `data/out/image_health.json`. Default DISABLED (#93 contract, consistency with every other automation even though read-only), daily 04:30.
- **The actual fix is in `/api/products`**: applies `image_health.clean_products` at REQUEST time — a review card is served WITHOUT any URL confirmed dead, so Chrome never gets handed the dead URL to request in the first place. `review_data.json` itself is never rewritten (unlike `resync_current`) — a dead image can silently come back to life on the CDN's own schedule and the next cache freshness cycle just serves it again, no resync step needed.
- RED `e74eb08` (18 pure tests: freshness window, consecutive-failure threshold, transient-failure survives, serve-time shallow-copy) → GREEN `9e09fb1`. Flask wiring (12 tests) + frontend tab in `844ea6f`/`0389235` (feature — tests mandatory, order flexible per tdd-workflow.md, not a strict bug-fix RED/GREEN pair). Version bump `7604325` 0.67.1→0.68.0.
- **Process note**: session started already checked out on local `main` (contrary to the two-branch workflow) — all 5 commits landed on `main` before the mistake was caught pre-push; fixed via `git checkout dev && git merge --ff-only main` (safe fast-forward, content identical) then `git checkout -B main origin/main` to reset the never-pushed local `main` back to the remote tip. No history was rewritten on any REMOTE ref.
- 703 backend + 62 e2e green (coverage 94.06%, limit 80%), ruff clean. PR #166 (Closes #135), merge `23f21b0`, main CI green (test+e2e; version-check skipped on main). Deploy = `systemctl --user restart parovanie-web` on `main`; data-safety counts PRED==PO (`decisions` 2831, `ordered_items` 146, `order_pairings` 57, `waiting_items` 11, `supplier_assignments` 1, `automations` 4, `review_data.json` 2857 — all unchanged; `image_health.json` still absent, automation never run in prod by design). DOM `verzia v0.68.0` confirmed live on `parovanie-forestshop.newlevel.media`; new "Kontrola obrázkov" tab renders (Zastavené, denne o 04:30, "zatiaľ nikdy"); review tab still renders all product images (78 `<img>` on-screen, real cdn.myshoptet.com URLs — `/api/products` filter is a no-op until the automation's first real run); console clean on both tabs. Playbook: new "VALIDÁCIA + serve-time filter automatizácia" pattern section + the `automations_server` e2e fixture's 0-products gotcha added to `.claude/skills/webreview`.
