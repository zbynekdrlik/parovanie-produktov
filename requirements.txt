# Pinned for reproducible CI (a new ruff/pytest release must not turn CI red
# with no code change). Bump deliberately.
requests==2.34.2
beautifulsoup4==4.15.0
lxml==6.1.1
rapidfuzz==3.14.5
pytest==9.1.1
flask==3.1.3
ruff==0.15.19
pytest-cov==7.1.0
